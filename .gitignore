# nix build outputs
/result
/result-*

# devenv state
/.devenv/
/.devenv.flake.nix
/.devenv-base-seeded

# bind-mounted read-only from devenv-base at container launch; not project-owned
/devenv.nix
/devenv.yaml
/devenv.lock

# local machine-specific config -- absolute paths, not portable.
# copy .mcp.json.example -> .mcp.json and edit for your machine
/.mcp.json
/devenv.local.nix

# agent sandbox / editor local state
/.ai-sandbox/
/.claude/settings.local.json
/.eva/
